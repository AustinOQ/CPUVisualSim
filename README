To-Do:
    Allow for a different branch predictor. 
    Save what the bp predictedin the statequeue and compare it to 
        the resolved value. 

    Add easy to pares outputs. 
        Output to output.txt.

   Comits to arch state should only happen at end of cycle. Currently, registers can be changes in exe phase. 
